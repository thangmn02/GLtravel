---
import FullScreenLayout from '../layouts/FullScreenLayout.astro';
import AttractionFeed from '../components/AttractionFeed';
import { attractions } from '../data/attractions';
import site from '../data/site';
---

<FullScreenLayout title={`Attractions — ${site.titleEn}`}>
  <AttractionFeed client:load attractions={attractions} />
</FullScreenLayout>
